import styled from "styled-components"

const CodeDemo = styled.div`
  border: 1px solid ${props => props.theme.toggleIcon};
  padding: 16px;
  background-color: ${props => props.theme.cardBackground};
`
export default CodeDemo
